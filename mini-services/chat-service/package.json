{
  "name": "beautyvote-chat-service",
  "version": "1.0.0",
  "description": "BeautyVote Live Chat Service — WebSocket + Telegram support bot",
  "private": true,
  "scripts": {
    "dev": "bun --hot index.ts",
    "start": "bun index.ts",
    "setup": "bun setup.ts",
    "postinstall": "bunx prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.11.1"
  },
  "devDependencies": {
    "prisma": "^6.11.1"
  }
}
