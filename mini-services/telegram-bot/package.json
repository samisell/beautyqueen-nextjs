{
  "name": "beautyvote-telegram-bot",
  "version": "1.0.0",
  "description": "BeautyVote Telegram Bot — handles /start command and opens the web app",
  "private": true,
  "scripts": {
    "dev": "bun --hot index.ts",
    "start": "bun index.ts",
    "setup": "bun setup.ts"
  },
  "dependencies": {
    "bun": "^1.2.0"
  }
}
